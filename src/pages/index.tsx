import Head from 'next/head';

import Particles from 'containers/home/particles';

const Home: React.FC = () => (
  <div>
    <Head>
      <title>NCS - GO</title>
    </Head>

    <Particles />
  </div>
);

export default Home;
